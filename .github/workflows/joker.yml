name: 自动生成 joker.m3u

on:
  workflow_dispatch:

  schedule:
    - cron: '0 22 * * *'
    - cron: '0 9 * * *'

  push:
    paths:
      - 'scripts/joker.py'
      - '.github/workflows/joker.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: 🐍 设置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 安装依赖
        run: pip install requests aiohttp

      - name: 🚀 运行脚本
        run: |
          python scripts/joker.py

      - name: 🔍 检查输出文件
        run: |
          echo "===== 当前目录 ====="
          pwd
          echo "===== 仓库根目录文件 ====="
          ls -la
          echo "===== scripts 目录 ====="
          ls -la scripts || true
          echo "===== 查找 joker.m3u ====="
          find . -name "joker.m3u" -type f || true
          echo "===== 文件预览 ====="
          head -n 20 joker.m3u || true

      - name: 📦 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: joker-m3u
          path: joker.m3u
          if-no-files-found: warn

      - name: 📤 提交更新
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

          if [ ! -f joker.m3u ]; then
            echo "未找到 joker.m3u，退出"
            exit 1
          fi

          git add joker.m3u

          if git diff --cached --quiet; then
            echo "无变化"
          else
            git commit -m "🤖 自动更新 joker.m3u"
            git push origin ${{ github.ref_name }}
          fi
