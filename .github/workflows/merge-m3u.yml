name: 自动合并M3U（抓取全网通港澳台）

on:
  # 手动触发
  workflow_dispatch:
  
  # 定时触发：北京时间 06:00 和 18:00
  schedule:
    - cron: '0 22 * * *'  # UTC 22:00 = 北京时间 06:00
    - cron: '0 10 * * *'  # UTC 10:00 = 北京时间 18:00
  
  # 当脚本更新时也触发
  push:
    paths:
      - 'scripts/merge_from_proxy.py'
      - '.github/workflows/merge-m3u.yml'

jobs:
  merge-m3u:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检出代码（带深度）
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0  # 获取完整历史
    
    - name: 🐍 设置Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: 📦 安装依赖
      run: |
        pip install requests beautifulsoup4
    
    - name: 🕒 显示时间信息
      run: |
        echo "=== 时间信息 ==="
        echo "当前UTC时间: $(date -u '+%Y-%m-%d %H:%M:%S')"
        echo "当前北京时间: $(date -d '+8 hours' '+%Y-%m-%d %H:%M:%S')"
        echo "工作流名称: ${{ github.workflow }}"
        echo "触发事件: ${{ github.event_name }}"
        echo "触发时间: ${{ github.event.schedule || '手动触发' }}"
    
    - name: 🚀 运行合并脚本
      run: |
        echo "开始运行合并脚本..."
        python scripts/merge_from_proxy.py
        
        echo -e "\n=== 检查生成的文件 ==="
        if [ -f "CC.m3u" ]; then
          echo "✅ CC.m3u 已生成"
          echo "文件大小: $(wc -c < CC.m3u) 字节"
          echo "行数: $(wc -l < CC.m3u)"
          echo "生成时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          echo -e "\n=== 文件内容预览 ==="
          echo "前10行:"
          head -10 CC.m3u
          echo "..."
          
          echo -e "\n=== 文件结构验证 ==="
          echo "EPG信息:"
          grep -i "url-tvg" CC.m3u || echo "未找到EPG信息"
          echo "全网通港澳台频道数: $(grep -c '全网通港澳台' CC.m3u || echo 0)"
          
          echo -e "\n=== 频道统计 ==="
          echo "BB频道数: $(grep -c '#EXTINF:' CC.m3u | tail -1 || echo 0)"
          
          # 显示几个具体的频道
          echo -e "\n=== 港澳台频道示例 ==="
          grep -A1 '全网通港澳台' CC.m3u | head -6
        else
          echo "❌ CC.m3u 未生成"
          exit 1
        fi
    
    - name: 🔄 同步远程更改
      run: |
        echo "同步远程仓库..."
        # 配置Git用户
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git config user.name "github-actions[bot]"
        
        # 拉取最新更改（使用rebase避免合并提交）
        git pull origin main --rebase --autostash
        
        echo "✅ 同步完成"
    
    - name: 📤 提交更新
      run: |
        echo "准备提交更新..."
        
        # 检查文件是否存在
        if [ ! -f "CC.m3u" ]; then
          echo "❌ CC.m3u不存在，无法提交"
          exit 1
        fi
        
        # 检查文件是否有变化
        if git diff --quiet CC.m3u; then
          echo "📭 CC.m3u 无变化，无需提交"
          exit 0
        fi
        
        # 添加文件
        git add CC.m3u
        
        # 创建提交信息
        TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M UTC')
        # 获取频道统计
        BB_COUNT=$(grep -c '#EXTINF:' CC.m3u 2>/dev/null || echo 0)
        GAT_COUNT=$(grep -c '全网通港澳台' CC.m3u 2>/dev/null || echo 0)
        TOTAL=$((BB_COUNT + GAT_COUNT))
        
        COMMIT_MSG="🤖 自动更新 CC.m3u [$TIMESTAMP]
        
        统计信息:
        • BB频道: $BB_COUNT
        • 全网通港澳台: $GAT_COUNT
        • 总计: $TOTAL"
        
        # 提交
        git commit -m "$COMMIT_MSG"
        
        echo "提交信息:"
        git log -1 --oneline
        
        # 推送更改
        echo "推送更改..."
        git push origin main
        
        echo "✅ 提交完成"
    
    - name: 📊 输出结果
      if: always()
      run: |
        echo "=== 运行总结 ==="
        echo "🕒 运行完成时间: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
        echo "🕐 北京时间: $(date -d '+8 hours' '+%Y-%m-%d %H:%M:%S')"
        echo ""
        echo "📅 下次计划运行:"
        echo "  • UTC 22:00 (北京时间 06:00)"
        echo "  • UTC 10:00 (北京时间 18:00)"
        echo ""
        echo "🔗 生成的文件:"
        echo "  • GitHub: https://github.com/${{ github.repository }}/blob/main/CC.m3u"
        echo "  • Raw: https://raw.githubusercontent.com/${{ github.repository }}/main/CC.m3u"
        echo ""
        echo "⚙️  配置信息:"
        echo "  • GitHub源: https://raw.githubusercontent.com/Jsnzkpg/Jsnzkpg/main/Jsnzkpg1"
        echo "  • BB源: https://raw.githubusercontent.com/sufernnet/joker/main/BB.m3u"
        echo "  • 搜索关键词: 全网通港澳台"
        echo "  • 脚本文件: scripts/merge_from_proxy.py"
        echo "  • 工作流文件: .github/workflows/merge-m3u.yml"
        echo "  • 更新频率: 每天2次 (06:00 和 18:00 北京时间)"
        echo ""
        echo "📊 运行状态:"
        if [ -f "CC.m3u" ]; then
          echo "  ✅ CC.m3u 生成成功"
          LINES=$(wc -l < CC.m3u)
          SIZE=$(wc -c < CC.m3u)
          GAT_COUNT=$(grep -c '全网通港澳台' CC.m3u || echo 0)
          BB_COUNT=$(grep -c '#EXTINF:' CC.m3u || echo 0)
          TOTAL=$((BB_COUNT + GAT_COUNT))
          
          echo "    行数: $LINES"
          echo "    大小: $((SIZE / 1024)) KB"
          echo "    BB频道数: $BB_COUNT"
          echo "    全网通港澳台频道数: $GAT_COUNT"
          echo "    总频道数: $TOTAL"
          
          # 检查EPG状态
          if grep -q 'url-tvg=' CC.m3u; then
            EPG_URL=$(grep -o 'url-tvg="[^"]*"' CC.m3u | head -1 | sed 's/url-tvg="//' | sed 's/"//')
            echo "    EPG状态: ✅ 正常"
            echo "    EPG地址: $EPG_URL"
          else
            echo "    EPG状态: ❌ 无EPG"
          fi
          
          # 显示更新时间
          UPDATE_TIME=$(grep -o '# 生成时间: [0-9:-]*' CC.m3u | head -1 | sed 's/# 生成时间: //')
          if [ -n "$UPDATE_TIME" ]; then
            echo "    更新时间: $UPDATE_TIME (北京时间)"
          fi
        else
          echo "  ❌ CC.m3u 生成失败"
          echo "  ℹ️  请检查 scripts/merge_from_proxy.py 脚本"
        fi
        
        echo ""
        echo "🔍 调试信息:"
        echo "  仓库: ${{ github.repository }}"
        echo "  分支: ${{ github.ref }}"
        echo "  触发者: ${{ github.actor }}"
        echo "  运行ID: ${{ github.run_id }}"
        echo "  运行号: ${{ github.run_number }}"
        
        echo ""
        echo "🔗 相关链接:"
        echo "  • 工作流运行: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        echo "  • 提交历史: https://github.com/${{ github.repository }}/commits/main/CC.m3u"
        echo "  • 原始BB源: https://raw.githubusercontent.com/sufernnet/joker/main/BB.m3u"
        echo "  • 原始GitHub源: https://raw.githubusercontent.com/Jsnzkpg/Jsnzkpg/main/Jsnzkpg1"
        
        echo ""
        echo "🎯 使用说明:"
        echo "  1. CC.m3u 可直接用于支持M3U格式的播放器"
        echo "  2. 文件每天自动更新两次"
        echo "  3. 如需手动更新，请在Actions页面点击 'Run workflow'"
        echo "  4. 查看详细日志请访问工作流运行页面"
