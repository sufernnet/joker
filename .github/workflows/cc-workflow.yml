name: CC自动更新工作流

on:
  # 手动触发
  workflow_dispatch:
  
  # 定时触发：北京时间 06:00 和 17:00
  schedule:
    - cron: '0 22 * * *'  # UTC 22:00 = 北京时间 06:00
    - cron: '0 9 * * *'   # UTC 09:00 = 北京时间 17:00
  
  # 当CC相关文件更新时触发
  push:
    paths:
      - 'cc_merge.py'
      - '.github/workflows/cc-workflow.yml'

jobs:
  cc-update:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v3
    
    - name: 🐍 设置Python环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: 📦 安装Python依赖
      run: pip install requests
    
    - name: 🕒 显示当前时间
      run: |
        echo "=== CC更新工作流 ==="
        echo "开始时间（北京时间）: $(date -d '+8 hours' '+%Y-%m-%d %H:%M:%S')"
        echo "触发方式: ${{ github.event_name }}"
        echo "脚本文件: cc_merge.py"
        echo "输出文件: CC.m3u"
    
    - name: 🚀 运行CC合并脚本
      run: |
        echo "正在运行 cc_merge.py..."
        python cc_merge.py
        
        echo -e "\n=== 检查CC.m3u文件 ==="
        if [ -f "CC.m3u" ]; then
          echo "✅ CC.m3u 创建成功"
          echo "文件大小: $(wc -c < CC.m3u) 字节"
          echo "行数: $(wc -l < CC.m3u)"
          
          echo -e "\n=== 内容预览（前10行）==="
          head -10 CC.m3u
          echo "..."
          
          echo -e "\n=== 频道统计 ==="
          echo "HK频道: $(grep -c 'group-title="HK"' CC.m3u || echo 0)"
          echo "TW频道: $(grep -c 'group-title="TW"' CC.m3u || echo 0)"
          echo "总频道: $(grep -c '^#EXTINF:' CC.m3u || echo 0)"
        else
          echo "❌ 错误：CC.m3u 未创建"
          exit 1
        fi
    
    - name: 🔄 自动提交CC文件
      if: success()
      run: |
        echo "正在提交CC.m3u更新..."
        
        # 设置Git用户
        git config user.email "cc-bot@users.noreply.github.com"
        git config user.name "CC自动更新机器人"
        
        # 添加CC.m3u文件
        git add CC.m3u
        
        # 检查是否有变更
        if git diff --cached --quiet; then
          echo "📭 CC.m3u无变化，无需提交"
        else
          # 提交变更
          COMMIT_TIME=$(date -d '+8 hours' '+%Y-%m-%d %H:%M')
          git commit -m "🤖 CC自动更新 [$COMMIT_TIME 北京时间]"
          
          # 推送到仓库
          git push origin main
          echo "✅ CC.m3u已更新并提交"
        fi
    
    - name: 📊 CC更新报告
      if: always()
      run: |
        echo "=== CC更新报告 ==="
        echo "完成时间: $(date -d '+8 hours' '+%Y-%m-%d %H:%M:%S 北京时间')"
        echo ""
        echo "📁 文件状态:"
        if [ -f "CC.m3u" ]; then
          SIZE=$(wc -c < CC.m3u)
          LINES=$(wc -l < CC.m3u)
          echo "  ✅ CC.m3u - $SIZE 字节, $LINES 行"
          echo ""
          echo "🔗 直接链接:"
          echo "  https://raw.githubusercontent.com/${{ github.repository }}/main/CC.m3u"
        else
          echo "  ❌ CC.m3u - 文件不存在"
        fi
        echo ""
        echo "⏰ 下次更新:"
        echo "  • 北京时间 06:00"
        echo "  • 北京时间 17:00"
        echo ""
        echo "⚙️ 配置文件:"
        echo "  • 工作流: .github/workflows/cc-workflow.yml"
        echo "  • 脚本: cc_merge.py"
